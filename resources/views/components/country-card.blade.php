<div class="rounded shadow-sm bg-white overflow-hidden">
    <img src="{{ $image }}" alt="{{ $name }}" class="w-full h-32 object-cover" />
    <p class="p-2 text-center font-semibold">{{ $name }}</p>
</div>
